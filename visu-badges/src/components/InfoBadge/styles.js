import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container: {
        display: 'flex',
        width: 100,
        height: 200,
        alignContent: 'center',
        alignItems: 'center',
       
    }
})